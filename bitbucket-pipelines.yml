image: ruby:2.5

pipelines:
  custom: # Pipelines that are triggered manually
    show-uptime:
      - step:
          script:
            - gem install bundler:2.2.4
            - bundle install
            - ruby ssm-ops.rb -i development/Web,development/Worker -c "uptime"